{
  "name": "tatra-guide",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd client/tatra-app && npm install --include=dev && npm run build",
    "start": "cd server && node app.js"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
