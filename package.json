{
  "name": "tatra-guide",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd client/tatra-app && npm install && npm run build && cd ../../server && npm install",
    "start": "cd server && node app.js"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
